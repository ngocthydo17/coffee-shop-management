@model IEnumerable<Manage_Coffee.Models.Da>



@if (Model != null && Model.Any())
{
    <label style="color:#07522E; font-size:20px; font-weight:bold; margin-bottom:10px;">Chọn đá (bắt buộc):</label>

    <div class="size-options">
        @foreach (var size in Model)
        {
            <label class="size-button">
                <input type="radio" name="idda" value="@size.MaDa" />
                <div class="content">
                    <img src="~/css/icon/icons8-ice-48.png" alt="Cup Icon" class="size-icon" />
                    @size.Ten
                </div>
            </label>
        }
    </div>
}
else
{
    <p>Không có kích thước nào khả dụng.</p>
}

<style>
    .size-options {
        margin-left: 10px;
        display: flex;
        gap: 10px;
        width: 98%;
    }

    .size-button {
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: white;
        border: 2px solid #228B22;
        border-radius: 5px;
        width: 30%;
        height: 55px;
        cursor: pointer;
        transition: background-color 0.3s, border-color 0.3s, color 0.3s;
        box-sizing: border-box;
    }

        .size-button input {
            display: none;
        }

    .size-icon {
        width: 30px;
        height: 30px;
        margin-right: 10px;
        filter: brightness(0) saturate(100%) invert(38%) sepia(64%) saturate(2724%) hue-rotate(90deg) brightness(90%) contrast(89%);
    }

    .size-button:hover {
        background-color: #e0f0e0;
        border-color: #66c066;
    }

    .size-button input:checked + .content {
        background-color: #006400;
        border-color: #006400;
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        width: 100%;
        height: 100%;
        padding: 0;
        box-sizing: border-box;
    }

        .size-button input:checked + .content .size-icon {
            filter: none;
        }

        .size-button input:checked + .content span {
            color: white;
        }
</style>
